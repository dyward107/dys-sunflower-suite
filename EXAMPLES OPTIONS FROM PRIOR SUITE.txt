
Add Google Fonts (Sirivennela + Quicksand)
Sirivennela → only for the text Dy’s Sunflower Suite

Quicksand → for literally everything else (Default App Font + Background)

The Case management mockup is attached 
1. A branded header bar with subtle sunflower illustration (free SVGs)

We add:

A thin header strip with a soft gradient

Subtle hand-drawn or realistic sunflower accents in SVG (light opacity 10–18%)

Logo text: Dy’s Sunflower Suite in a custom free Google font (“Fraunces”, “Merriweather”, “Cormorant”, or “Playfair Display”)

A small sunflower icon integrated into the title

2. Soft background gradient instead of flat yellow

Replace the flat background with a two-tone gradient using your palette:

bg-gradient-to-br from-sunflower-cream to-sunflower-beige


This IMMEDIATELY makes the app feel premium.

3. Card redesigns using glassmorphism + taupe borders

Use:

rounder corners

soft inner shadows

taupe border

white overlay at 75% opacity to reduce the yellow overload

4. Replace big solid color tables with soft alternating rows

Use:

white + beige alternating rows

taupe 20% borders

sunflower gold header strip

scroll shadow at the top

5. Add floral corner overlays (free SVGs) with low opacity

Corner botanical accents like this:

Top-left: faint sunflower sketch

Bottom-right: light watercolor leaf

Opacity 6–10%

Blurred using CSS

These do NOT interfere with text and look sophisticated.

6. Add motion + micro-interactions

Button hover → tiny lift + shadow

Case row hover → soft beige highlight

Page transitions → fade & scale

7. Update typography

Use:

Header font: Fraunces or Playfair Display for elegant serif branding

Body: Inter or Source Sans Pro

Accents: uppercase brown text for section titles

🌻 WHAT THIS WILL LOOK LIKE



New Header


╔═════════════════════════════════════════════════════════════════╗
║   🌻 Dy’s Sunflower Suite                                       ║
║   [soft watercolor sunflower fading behind title]		  ║
║   Volumn Case Management System (in smaller font                ║
╚═════════════════════════════════════════════════════════════════╝


New Page Background

A soft gradient, not blocks:

Cream (#FFF9C4) → Beigey gold (#FFECB3)
with botanical corner art

New Case Manager Table

thin brown header text

soft green “open” badges

elegant serif titles

rows with alternating opacity

New Case Detail Page

two-column layout

card-style section groups

subtle florals behind the content, barely visible

elegant headers

soft shadowed containers

“Edit Case” button now gold, sleek, lifted on hover


Attached Mockup for the Case Information Screen — fully upgraded, sophisticated, elegant, and ZERO sticky-note energy. 🌻

✔ No more “yellow sticky note” card
✔ Cleaner, more structured field layout
✔ Soft realistic sunflower & botanical corners
✔ Subtle background gradients
✔ Modern two-column sectioning
✔ Actual visual hierarchy
✔ Soft shadows & refined border radiuses
✔ Elegant header with branding

This is the exact same level of aesthetic as the Case Manager mockup attached

5. Boxed Sections for Case Details (Option B)

Now we upgrade the Case Information / Case Detail screen.

Assuming you have a component like CaseDetail.tsx or similar. If not, use the file where you render the individual case info.

File: src/components/CaseDetail.tsx (name may differ — pick the correct one)

In Cursor:

Open the case detail component.

Replace the main sticky-note style container with boxed cards like this:

import React from "react";

export const CaseDetail: React.FC = () => {
  // Replace with real data props/state
  return (
    <div className="max-w-5xl mx-auto space-y-6">

      {/* Back link + title row */}
      <div className="flex items-center justify-between mb-2">
        <button className="text-sunflower-brown/80 text-sm hover:underline">
          ← Back to Cases
        </button>

        <button className="px-4 py-2 rounded-full bg-sunflower-gold text-white font-medium shadow-sm hover:bg-sunflower-brown transition">
          Edit Case
        </button>
      </div>

      {/* Case header box */}
      <section className="rounded-3xl bg-white/80 border border-sunflower-taupe/60 shadow-md backdrop-blur-sm px-6 py-5">
        <h2 className="font-base text-2xl md:text-3xl text-sunflower-brown font-semibold">
          Test v. Case
        </h2>
        <p className="text-sunflower-brown/70 mt-1">CM# 1234.5678</p>
      </section>

      {/* CASE DETAILS box */}
      <section className="rounded-3xl bg-white/80 border border-sunflower-taupe/60 shadow-md backdrop-blur-sm px-6 py-5">
        <h3 className="font-base text-lg text-sunflower-brown font-semibold mb-4">
          CASE DETAILS
        </h3>

        <div className="grid grid-cols-1 md:grid-cols-2 gap-x-10 gap-y-4">
          <div>
            <p className="text-sunflower-brown font-semibold text-sm">
              Lead Attorney
            </p>
            <p className="text-sunflower-brown/90 text-sm">
              Rebecca Strickland
            </p>
          </div>

          <div>
            <p className="text-sunflower-brown font-semibold text-sm">
              Phase
            </p>
            <span className="inline-flex items-center px-3 py-1 mt-1 rounded-full bg-sunflower-green/70 text-sunflower-brown text-xs font-semibold">
              Open
            </span>
          </div>

          <div>
            <p className="text-sunflower-brown font-semibold text-sm">
              Status
            </p>
            <p className="text-sunflower-brown/90 text-sm">
              Pre-Suit/Intake
            </p>
          </div>

          <div>
            <p className="text-sunflower-brown font-semibold text-sm">
              Case Type
            </p>
            <p className="text-sunflower-brown/90 text-sm">
              Motor Vehicle Accident – Uninsured/Underinsured Motorist
            </p>
          </div>

          <div>
            <p className="text-sunflower-brown font-semibold text-sm">
              Date Opened
            </p>
            <p className="text-sunflower-brown/90 text-sm">11/12/2025</p>
          </div>

          <div>
            <p className="text-sunflower-brown font-semibold text-sm">
              Date of Loss
            </p>
            <p className="text-sunflower-brown/90 text-sm">04/12/2025</p>
          </div>
        </div>
      </section>

      {/* VENUE box */}
      <section className="rounded-3xl bg-white/80 border border-sunflower-taupe/60 shadow-md backdrop-blur-sm px-6 py-5">
        <h3 className="font-base text-lg text-sunflower-brown font-semibold mb-4">
          VENUE
        </h3>

        <div className="grid grid-cols-1 md:grid-cols-2 gap-x-10 gap-y-4">
          <div>
            <p className="text-sunflower-brown font-semibold text-sm">Court</p>
            <p className="text-sunflower-brown/90 text-sm">Fulton County</p>
          </div>

          <div>
            <p className="text-sunflower-brown font-semibold text-sm">Judge</p>
            <p className="text-sunflower-brown/90 text-sm">Roth</p>
          </div>

          <div>
            <p className="text-sunflower-brown font-semibold text-sm">
              Clerk
            </p>
            <p className="text-sunflower-brown/90 text-sm">Sally</p>
          </div>

          <div>
            <p className="text-sunflower-brown font-semibold text-sm">
              Staff Attorney
            </p>
            <p className="text-sunflower-brown/90 text-sm">Sam</p>
          </div>
        </div>
      </section>

      {/* You can add more boxed sections: Discovery, Damages, Notes, etc. */}
    </div>
  );
};

Make the Case Manager Page Match the New Style

If you want the Case Manager list to match E-style:

Wrap the table in a similar boxed section (bg-white/80, rounded-3xl, border-sunflower-taupe/60, shadow-md).

Use font-base (Quicksand) everywhere.

Keep buttons in bg-sunflower-gold text-white with hover:bg-sunflower-brown.

Add Subtle Botanical SVG Backgrounds Behind the Boxes

This will replicate the faint watercolor leaves and florals sitting behind the white/cream content cards.
Refactor Your Case Manager Table to Match the Premium Mockup

This will give you:

soft white/cream card container

gold table header with subtle shadow

alternating row colors

rounded edges

soft borders

elevated floating-table look
dd Botanical SVG Backgrounds Behind Boxes
Step 1A — Create an /assets/botanicals/ folder

In Cursor:

Right-click src/ → New Folder

Name it:

src/assets/botanicals

Step 1B — Add FREE subtle botanical SVGs

Paste the following files into that folder.

File 1: botanical-corner-left.svg
File 2: botanical-corner-right.svg
Step 1C — Add backgrounds behind content boxes

Wherever you have a content card like:

<section className="rounded-3xl bg-white/80 border border-sunflower-taupe/60 shadow-md backdrop-blur-sm px-6 py-5">...</section>


Replace it with:

<section className="relative rounded-3xl bg-white/80 border border-sunflower-taupe/60 shadow-md backdrop-blur-sm px-6 py-8 overflow-hidden">

  {/* Botanical BG Left */}
  <img
    src="/src/assets/botanicals/botanical-corner-left.svg"
    className="absolute top-0 left-0 w-48 h-48 opacity-20 pointer-events-none select-none"
    alt=""
  />

  {/* Botanical BG Right */}
  <img
    src="/src/assets/botanicals/botanical-corner-right.svg"
    className="absolute bottom-0 right-0 w-48 h-48 opacity-20 pointer-events-none select-none"
    alt=""
  />

  {/* The actual content */}
  <div className="relative z-10">
    ...content...
  </div>

</section>


This is exactly the soft leaf-and-watercolor effect you see in the mockups.

🌻 PART 2 — Refactor Case Manager Table to Match E-Style Mockup
Step 2A — Wrap the table in a premium container

Open:

src/components/CaseManager.tsx
or wherever the table is rendered


Replace your table wrapper with:

<div className="mt-6 rounded-3xl bg-white/80 shadow-md border border-sunflower-taupe/60 backdrop-blur-sm overflow-hidden">

Step 2B — Style the table header like the mockup

Replace your table thead with:

<thead className="bg-sunflower-gold/90 text-sunflower-brown text-sm font-semibold">
  <tr>
    <th className="px-4 py-3 text-left tracking-wide">CM NUMBER</th>
    <th className="px-4 py-3 text-left">CASE NAME</th>
    <th className="px-4 py-3 text-left">LEAD ATTORNEY</th>
    <th className="px-4 py-3 text-left">STATUS</th>
    <th className="px-4 py-3 text-left">PHASE</th>
    <th className="px-4 py-3 text-left">DATE OPENED</th>
  </tr>
</thead>

Step 2C — Style the rows like the mockup

Replace tbody rows with:

<tbody className="divide-y divide-sunflower-taupe/30">
  {cases.map((caseItem) => (
    <tr
      key={caseItem.cm_number}
      className="bg-white/60 hover:bg-sunflower-beige/40 transition"
    >
      <td className="px-4 py-3 text-sunflower-brown/90">{caseItem.cm_number}</td>
      <td className="px-4 py-3">{caseItem.case_name}</td>
      <td className="px-4 py-3">{caseItem.lead_attorney}</td>
      <td className="px-4 py-3">{caseItem.status}</td>
      <td className="px-4 py-3">
        <span className="inline-flex items-center px-3 py-1 rounded-full bg-sunflower-green/70 text-sunflower-brown text-xs font-semibold">
          {caseItem.phase}
        </span>
      </td>
      <td className="px-4 py-3">{caseItem.date_opened}</td>
    </tr>
  ))}
</tbody>


This gives you:

transparent soft rows

hover effect

subtle border separation

green badge for phase

overall clean UI

🌻 PART 3 — Add Flourished Botanical Background to Entire Page

At the top of your page (CaseManager + CaseDetail page), wrap your content with:

<div className="relative">

  {/* Background botanicals positioned in corners */}
  <img
    src="/src/assets/botanicals/botanical-corner-left.svg"
    className="absolute top-0 left-0 w-64 h-64 opacity-[0.15] pointer-events-none"
    alt=""
  />

  <img
    src="/src/assets/botanicals/botanical-corner-right.svg"
    className="absolute bottom-0 right-0 w-64 h-64 opacity-[0.15] pointer-events-none"
    alt=""
  />

  <div className="relative z-10">
    ....page content...
  </div>
</div>


This matches EXACTLY the mockups (E-style watercolor botanical shading).

🌻 PART 4 — Match Fonts to the Mockup
In index.css:
body {
  @apply font-base bg-gradient-to-br from-sunflower-cream to-sunflower-beige text-sunflower-brown;
}

Branding header (Sirivennela):
<h1 className="font-brand text-4xl text-sunflower-brown flex items-center gap-2">
  <span>🌻</span>
  Dy&apos;s Sunflower Suite
</h1>


Everything else uses font-base (Quicksand).

UPDATED FULL-PAGE BACKGROUND BOTANICALS

Replace your page wrapper with:

<div className="relative">

  <img
    src="/src/components/shared/botanical-corner-left.svg"
    className="absolute top-0 left-0 w-64 h-64 opacity-[0.15] pointer-events-none select-none"
    alt=""
  />

  <img
    src="/src/components/shared/botanical-corner-right.svg"
    className="absolute bottom-0 right-0 w-64 h-64 opacity-[0.15] pointer-events-none select-none"
    alt=""
  />

  <div className="relative z-10">
    {children /* or page content */}
  </div>
</div>

UPDATED CASE MANAGER TABLE HEADER
<thead className="bg-sunflower-gold/90 text-sunflower-brown text-sm font-semibold relative z-10">
  <tr>
    <th className="px-4 py-3 text-left tracking-wide">CM NUMBER</th>
    <th className="px-4 py-3 text-left">CASE NAME</th>
    <th className="px-4 py-3 text-left">LEAD ATTORNEY</th>
    <th className="px-4 py-3 text-left">STATUS</th>
    <th className="px-4 py-3 text-left">PHASE</th>
    <th className="px-4 py-3 text-left">DATE OPENED</th>
  </tr>
</thead>

🌻 UPDATED TABLE BODY (rows)
<tbody className="divide-y divide-sunflower-taupe/30 relative z-10">
  {cases.map((caseItem) => (
    <tr
      key={caseItem.cm_number}
      className="bg-white/60 hover:bg-sunflower-beige/40 transition"
    >
      <td className="px-4 py-3 text-sunflower-brown/90">{caseItem.cm_number}</td>
      <td className="px-4 py-3">{caseItem.case_name}</td>
      <td className="px-4 py-3">{caseItem.lead_attorney}</td>
      <td className="px-4 py-3">{caseItem.status}</td>
      <td className="px-4 py-3">
        <span className="inline-flex items-center px-3 py-1 rounded-full bg-sunflower-green/70 text-sunflower-brown text-xs font-semibold">
          {caseItem.phase}
        </span>
      </td>
      <td className="px-4 py-3">{caseItem.date_opened}</td>
    </tr>
  ))}
</tbody>


1. Create a shared Module A layout with botanical background

File: src/components/moduleA/ModuleALayout.tsx
(Create this file)

// src/components/moduleA/ModuleALayout.tsx
// Shared layout wrapper for all Module A screens (Case Manager, Case Detail, Case Form)

import React from 'react';

// Adjust these names to match your actual SVG filenames in src/components/shared
import TopLeftBotanical from '../shared/sunflower-top-left.svg';
import BottomRightBotanical from '../shared/sunflower-bottom-right.svg';

interface ModuleALayoutProps {
  title?: string;
  children: React.ReactNode;
}

export const ModuleALayout: React.FC<ModuleALayoutProps> = ({ title, children }) => {
  return (
    <div className="relative min-h-screen bg-gradient-to-br from-[#fff9c4] via-[#ffecb3] to-[#d7ccc8]">
      {/* Botanical background illustrations */}
      <img
        src={TopLeftBotanical}
        alt=""
        className="pointer-events-none select-none opacity-60 absolute -top-6 -left-4 w-52 md:w-64"
      />
      <img
        src={BottomRightBotanical}
        alt=""
        className="pointer-events-none select-none opacity-60 absolute -bottom-10 -right-6 w-52 md:w-64"
      />

      {/* Soft inner panel */}
      <div className="relative z-10 max-w-6xl mx-auto px-6 py-8 md:px-10 md:py-10">
        <div className="rounded-[32px] bg-[rgba(255,252,240,0.9)] shadow-[0_18px_45px_rgba(99,49,18,0.18)] border border-[#d7ccc8]/70 backdrop-blur-sm">
          <div className="px-6 py-6 md:px-10 md:py-8">
            {title && (
              <header className="mb-6 md:mb-8">
                <h1 className="text-3xl md:text-4xl font-semibold text-[#633112] tracking-wide">
                  {title}
                </h1>
              </header>
            )}
            {children}
          </div>
        </div>
      </div>
    </div>
  );
};

export default ModuleALayout;


If your SVG names differ, just have Cursor replace sunflower-top-left.svg and sunflower-bottom-right.svg with the real filenames.

2. Make the Case Manager (CaseList) match the mockup

File: src/components/moduleA/CaseList.tsx 

CaseList

2.1 Add the layout import

At the top with the other imports:

import { ModuleALayout } from './ModuleALayout';

2.2 Replace the entire return in CaseList with this

Keep all logic above (sortedCases, SortIcon, handlers, etc.) intact; only replace the return (…) block:
  return (
    <ModuleALayout title="Case Manager">
      <div className="space-y-6">
        {/* Top row: title + New Case button is already in the global header,
            so here we just keep the action buttons */}
        <div className="flex flex-col md:flex-row md:items-center md:justify-between gap-4">
          <div className="space-y-1">
            <p className="text-sm text-[#633112]/70">
              Search by case name, CM number, or party name.
            </p>
          </div>

          <div className="flex gap-3">
            <button
              onClick={handleNewCase}
              className="inline-flex items-center gap-2 rounded-full bg-[#E3A008] px-4 py-2 text-sm font-semibold text-white shadow-md shadow-[#E3A008]/40 hover:bg-[#c88606] focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-[#E3A008] focus:ring-offset-[#fff9c4]"
            >
              + New Case
            </button>
            <button
              type="button"
              onClick={() => setShowFilters(!showFilters)}
              className="rounded-full border border-[#d7ccc8] bg-white/80 px-4 py-2 text-sm font-medium text-[#633112] shadow-sm hover:bg-[#fff9c4] focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-[#E3A008] focus:ring-offset-[#fff9c4]"
            >
              {showFilters ? 'Hide Filters' : 'Show Filters'}
            </button>
          </div>
        </div>

        {/* Search Bar */}
        <form onSubmit={handleSearch} className="flex flex-col md:flex-row gap-3">
          <div className="flex-1">
            <input
              type="text"
              placeholder="Search by case name, CM number, or party name..."
              value={searchQuery}
              onChange={(e) => setSearchQuery(e.target.value)}
              className="w-full rounded-full border border-[#d7ccc8] bg-white/90 px-4 py-2.5 text-sm text-[#633112] shadow-inner shadow-white/60 placeholder:text-[#a27f67] focus:outline-none focus:ring-2 focus:ring-[#E3A008] focus:border-[#E3A008]"
            />
          </div>
          <button
            type="submit"
            className="inline-flex items-center justify-center rounded-full bg-[#E3A008] px-6 py-2.5 text-sm font-semibold text-white shadow-md shadow-[#E3A008]/40 hover:bg-[#c88606] focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-[#E3A008] focus:ring-offset-[#fff9c4]"
          >
            Search
          </button>
        </form>

        {/* Filters */}
        {showFilters && (
          <div className="rounded-3xl border border-[#d7ccc8]/80 bg-white/80 px-4 py-4 shadow-sm">
            <div className="grid grid-cols-1 md:grid-cols-4 gap-3">
              {/* Lead Attorney */}
              <div>
                <label className="block text-xs font-semibold uppercase tracking-wide text-[#633112]/80 mb-1">
                  Lead Attorney
                </label>
                <select
                  value={filters.lead_attorney || ''}
                  onChange={(e) => handleFilterChange('lead_attorney', e.target.value)}
                  className="w-full rounded-full border border-[#d7ccc8] bg-white px-3 py-2 text-sm text-[#633112] focus:outline-none focus:ring-2 focus:ring-[#E3A008]"
                >
                  <option value="">All</option>
                  {LEAD_ATTORNEYS.map((attorney) => (
                    <option key={attorney} value={attorney}>
                      {attorney}
                    </option>
                  ))}
                </select>
              </div>

              {/* Status */}
              <div>
                <label className="block text-xs font-semibold uppercase tracking-wide text-[#633112]/80 mb-1">
                  Status
                </label>
                <select
                  value={filters.status || ''}
                  onChange={(e) => handleFilterChange('status', e.target.value)}
                  className="w-full rounded-full border border-[#d7ccc8] bg-white px-3 py-2 text-sm text-[#633112] focus:outline-none focus:ring-2 focus:ring-[#E3A008]"
                >
                  <option value="">All Statuses</option>
                  {CASE_STATUSES.map((status) => (
                    <option key={status} value={status}>
                      {status}
                    </option>
                  ))}
                </select>
              </div>

              {/* Phase */}
              <div>
                <label className="block text-xs font-semibold uppercase tracking-wide text-[#633112]/80 mb-1">
                  Phase
                </label>
                <select
                  value={filters.phase || ''}
                  onChange={(e) =>
                    handleFilterChange('phase', e.target.value as 'Open' | 'Pending' | 'Closed')
                  }
                  className="w-full rounded-full border border-[#d7ccc8] bg-white px-3 py-2 text-sm text-[#633112] focus:outline-none focus:ring-2 focus:ring




OTHER OPTION FOR BOTANICAL THEMES
🌻 STEP 1 — CREATE THE GLOBAL BOTANICAL LAYOUT WRAPPER

This MUST wrap every Module A screen.

Create file:

src/components/moduleA/ModuleALayout.tsx


Paste this EXACT VERSION:

import React from "react";
import TopLeftBotanical from "../shared/botanical-corner-left.svg";
import BottomRightBotanical from "../shared/botanical-corner-right.svg";

export const ModuleALayout = ({ children, title }: any) => {
  return (
    <div className="relative min-h-screen w-full bg-gradient-to-br from-[#FFF9C4] via-[#FFECB3] to-[#FFF2C0] overflow-hidden">

      {/* Botanical Background - TOP LEFT */}
      <img
        src={TopLeftBotanical}
        aria-hidden="true"
        className="pointer-events-none select-none absolute top-0 left-0 w-[420px] opacity-[0.25]"
      />

      {/* Botanical Background - BOTTOM RIGHT */}
      <img
        src={BottomRightBotanical}
        aria-hidden="true"
        className="pointer-events-none select-none absolute bottom-0 right-0 w-[420px] opacity-[0.25]"
      />

      {/* CONTENT CONTAINER */}
      <div className="relative z-20 max-w-7xl mx-auto px-8 py-10">
        {title && (
          <h1 className="font-brand text-[42px] text-[#633112] mb-6 flex items-center gap-3">
            <img
              src="/src/components/shared/sunflower-icon.svg"
              className="w-10 h-10"
            />
            {title}
          </h1>
        )}
        {children}
      </div>
    </div>
  );
};

export default ModuleALayout;


This gives the mockup-level background, not the pale egg Cursor dumped.

🌻 STEP 2 — REPLACE YOUR CASELIST WITH A REAL MOCKUP VERSION

Open:

src/components/moduleA/CaseList.tsx


DO NOT change state logic or table logic — just replace your return (…) with this:

return (
  <ModuleALayout title="Case Manager">
    <div className="space-y-6">

      {/* TOP RIGHT BUTTONS */}
      <div className="flex justify-end gap-4">
        <button
          onClick={handleNewCase}
          className="rounded-xl bg-[#E3A008] text-white px-5 py-2.5 text-sm font-medium shadow-[0_3px_8px_rgba(227,160,8,0.35)] hover:bg-[#c98b06] transition"
        >
          + New Case
        </button>

        <button
          className="rounded-xl border border-[#d7ccc8] bg-white px-5 py-2.5 text-sm font-medium text-[#633112] shadow-sm hover:bg-[#fff9c4] transition"
          onClick={() => setShowFilters(!showFilters)}
        >
          {showFilters ? "Hide Filters" : "Show Filters"}
        </button>
      </div>

      {/* SEARCH BAR + BUTTON */}
      <div className="flex gap-4">
        <input
          placeholder="Search by case name, CM number, or party name..."
          value={searchQuery}
          onChange={(e) => setSearchQuery(e.target.value)}
          className="flex-1 rounded-2xl bg-white/90 border border-[#d7ccc8] px-5 py-3 text-[#633112] text-sm shadow-inner focus:ring-2 focus:ring-[#E3A008]"
        />

        <button
          onClick={handleSearch}
          className="rounded-xl bg-[#E3A008] text-white px-6 py-3 text-sm font-medium shadow-md shadow-[#e3a00840] hover:bg-[#c98b06] transition"
        >
          Search
        </button>
      </div>

      {/* TABLE WRAPPER */}
      <div className="rounded-3xl bg-white/80 border border-[#d7ccc8] shadow-[0_10px_25px_rgba(99,49,18,0.15)] overflow-hidden">

        {/* HEADER */}
        <div className="grid grid-cols-6 bg-[#E3A008] text-[#633112] text-sm font-semibold py-3 px-4">
          <div>CM NUMBER</div>
          <div>CASE NAME</div>
          <div>LEAD ATTORNEY</div>
          <div>STATUS</div>
          <div>PHASE</div>
          <div>DATE OPENED</div>
        </div>

        {/* ROWS */}
        {sortedCases.map((c) => (
          <div
            key={c.cm_number}
            className="grid grid-cols-6 bg-white/70 hover:bg-[#FFF2C0] transition py-3 px-4 border-b border-[#d7ccc8]/40 text-[#633112]"
          >
            <div>{c.cm_number}</div>
            <div>{c.case_name}</div>
            <div>{c.lead_attorney}</div>
            <div>{c.status}</div>
            <div>
              <span className="inline-block px-3 py-1 bg-[#AED581] text-[#4a6628] text-xs rounded-full">
                {c.phase}
              </span>
            </div>
            <div>{c.date_opened}</div>
          </div>
        ))}
      </div>
    </div>
  </ModuleALayout>
);


This matches the mockup EXACTLY.

🌻 STEP 3 — CASE DETAIL (the other mockup)

Open:

src/components/moduleA/CaseDetail.tsx


Replace the <return> with this:

return (
  <ModuleALayout title="Dy’s Sunflower Suite">

    <button
      onClick={() => navigate("/cases")}
      className="text-[#633112] mb-6 hover:underline"
    >
      ← Back to Cases
    </button>

    <div className="rounded-[32px] bg-white/85 border border-[#d7ccc8]/70 shadow-[0_12px_30px_rgba(99,49,18,0.15)] backdrop-blur-sm p-8 relative overflow-hidden">

      {/* Inner botanicals */}
      <img
        src={TopLeftBotanical}
        className="absolute top-0 left-0 w-52 opacity-[0.18] pointer-events-none"
        alt=""
      />
      <img
        src={BottomRightBotanical}
        className="absolute bottom-0 right-0 w-52 opacity-[0.18] pointer-events-none"
        alt=""
      />

      <div className="relative z-10">

        <h2 className="text-[32px] font-semibold text-[#633112]">
          {caseData.case_name}
        </h2>
        <p className="text-[#633112]/70 mb-6">CM# {caseData.cm_number}</p>

        {/* DETAILS GRID */}
        <div className="grid grid-cols-2 gap-y-6 gap-x-16">

          <div>
            <p className="text-sm font-semibold text-[#633112]">Lead Attorney</p>
            <p className="text-[#633112]/80">{caseData.lead_attorney}</p>
          </div>

          <div>
            <p className="text-sm font-semibold text-[#633112]">Status</p>
            <p className="text-[#633112]/80">{caseData.status}</p>
          </div>

          <div>
            <p className="text-sm font-semibold text-[#633112]">Date Opened</p>
            <p className="text-[#633112]/80">{caseData.date_opened}</p>
          </div>

          <div>
            <p className="text-sm font-semibold text-[#633112]">Date of Loss</p>
            <p className="text-[#633112]/80">{caseData.date_of_loss}</p>
          </div>

          <div>
            <p className="text-sm font-semibold text-[#633112]">Phase</p>
            <span className="inline-block bg-[#AED581] text-[#4a6628] px-3 py-1 rounded-full text-xs">
              {caseData.phase}
            </span>
          </div>

        </div>

        {/* EDIT BUTTON */}
        <div className="mt-10">
          <button
            onClick={() => navigate(`/cases/${caseData.cm_number}/edit`)}
            className="rounded-xl bg-[#E3A008] text-white px-6 py-3 shadow-md hover:bg-[#c98b06] transition"
          >
            Edit Case
          </button>
        </div>

      </div>
    </div>

  </ModuleALayout>
);

🌻 STEP 4 — CASE FORM, ADD PARTY, ADD POLICY

We will style ALL of them with the same pattern:

✓ White rounded card
✓ Inner botanicals
✓ Soft shadow
✓ Sunflower buttons
✓ Clean inputs

Below is the generic version Cursor can apply to ALL your forms:

🌻 FORM WRAPPER (Paste into CaseForm, AddPartyModal, AddPolicyModal)
<ModuleALayout title="Case Information">

  <div className="rounded-[32px] bg-white/85 border border-[#d7ccc8] shadow-[0_10px_30px_rgba(99,49,18,0.15)] backdrop-blur-sm p-10 relative overflow-hidden">

    {/* Inner botanicals */}
    <img
      src={TopLeftBotanical}
      className="absolute top-0 left-0 w-48 opacity-[0.16] pointer-events-none"
      alt=""
    />
    <img
      src={BottomRightBotanical}
      className="absolute bottom-0 right-0 w-48 opacity-[0.16] pointer-events-none"
      alt=""
    />

    {/* FORM CONTENT */}
    <div className="relative z-10">
      {/* Your form fields go here */}
    </div>
  </div>

</ModuleALayout>
